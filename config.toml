baseURL = "https://kontext.dev/"
title = "Kontext — CI/CD Friendly API Documentation"
languageCode = "en-us"

# Disable unused features
disableKinds = ["taxonomy", "term", "RSS", "sitemap"]

[params]
  description = "Auto-sync API documentation for FastAPI. CI/CD native, developer peace of mind."
  github_repo = "https://github.com/kontext-ai/kontext"

[markup]
  [markup.highlight]
    codeFences = true
    guessSyntax = false
    lineNos = false
    noClasses = true
    style = "github"
    tabWidth = 2
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[outputs]
  home = ["HTML"]
  section = ["HTML"]
  page = ["HTML"]

